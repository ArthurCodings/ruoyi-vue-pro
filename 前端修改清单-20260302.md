# 前端修改清单（2026-03-02）

## 一、员工薪资档案

**页面/路由**：人事专栏 → 员工薪资档案（新增/编辑表单）

**新增表单项**（在「补贴」与「生效日期」之间）：
| 字段 | 类型 | 说明 |
|------|------|------|
| socialInsurance | number | 五险（元） |
| housingFund | number | 公积金（元） |
| tax | number | 个税（元） |
| other | number | 其他（元，杂项扣款） |
| otherRemark | string | 其他扣款备注说明 |

**接口**：`/system/employee-salary/create`、`/system/employee-salary/update`  
**请求体**：在 `EmployeeSalarySaveReqVO` 中增加上述 4 个字段  
**响应体**：`EmployeeSalaryRespVO` 中已包含上述 4 个字段

---

## 二、花名册（人员档案）

**页面/路由**：人事专栏 → 花名册（新增/编辑表单）

**新增表单项**（在「离职日期」与「身份证号」之间）：
| 字段 | 类型 | 说明 |
|------|------|------|
| bankCardNo | string | 银行卡号 |
| bankName | string | 开户行 |
| education | string | 学历（如：本科、硕士） |
| currentAddress | string | 现居住地 |
| householdType | string | 户口类型（如：城镇、农村） |

**已有字段**：`formalDate`（转正日期）在入职日期与离职日期之间，需确保表单中有该字段且顺序正确。

**接口**：`/system/hr-employee/create`、`/system/hr-employee/update`  
**请求体**：`HrEmployeeSaveReqVO` 中增加上述 5 个字段  
**响应体**：`HrEmployeeRespVO` 中已包含上述 5 个字段及 `formalDate`

---

## 三、合同管理

**页面/路由**：合同管理（新增/编辑表单）

**新增表单项**（在「合同类型」之后）：
| 字段 | 类型 | 说明 |
|------|------|------|
| serviceType | number | 服务类型：1=长期，2=短期 |

**接口**：`/infra/contract/create`、`/infra/contract/update`  
**请求体**：`InfraContractSaveReqVO` 中增加 `serviceType`  
**响应体**：`InfraContractRespVO` 中已包含 `serviceType`

---

## 四、月度薪资

**页面/路由**：人事专栏 → 月度薪资（列表、详情、计算明细弹窗）

**列表/详情**：`SalaryMonthlyRespVO` 新增字段需在表格或详情中展示：
| 字段 | 说明 |
|------|------|
| socialInsurance | 五险 |
| housingFund | 公积金 |
| tax | 个税 |
| other | 其他 |

**薪资计算明细弹窗**：后端 `salaryDetail` JSON 已包含五险、公积金、个税、其他四项，前端按 `items` 数组顺序渲染即可，无需额外处理。

---

## 五、在线文档分类

**修改逻辑**：调用 `PUT /infra/doc/category/update` 时，**必须**在请求体中携带 `id`，否则会返回「文档分类不存在」错误。  
**建议**：编辑分类时，从列表/树节点获取当前分类 `id`，确保提交时一并传入。

---

## 六、法定节假日

**后端**：法定节假日修改逻辑已检查，无异常。若仍有问题，请排查前端是否在 `PUT /system/holiday/update` 时正确传入 `id` 及完整表单数据。

---

## 七、字段顺序参考

**花名册表单建议顺序**：  
入职日期 → 转正日期 → 离职日期 → 银行卡号 → 开户行 → 学历 → 现居住地 → 户口类型 → 身份证号 → …

**员工薪资档案表单建议顺序**：  
基本工资 → 岗位工资 → 补贴 → 五险 → 公积金 → 个税 → 其他 → 个人全勤奖 → 生效日期 → …
