# 若依 RuoYi-Vue-Pro 部署指南

> 针对公网服务器 8.155.49.186，使用 Docker 部署 MySQL、Redis 及后端应用。

---

## 一、运行哪个模块 / 打哪个包

项目是 Maven 多模块，但**只有一个主入口**：

| 模块 | 作用 | 是否打包运行 |
|------|------|--------------|
| **yudao-server** | 主应用入口，聚合 system、infra、bpm 等 | ✅ **只打这个** |
| yudao-module-system | 系统模块（含 IM） | 作为依赖被 server 引入 |
| yudao-module-infra | 基础设施模块 | 同上 |
| yudao-module-bpm | 工作流模块 | 同上 |

**结论：只需打包并运行 `yudao-server`，其它模块不用单独打包。**

---

## 二、打包命令

在项目根目录执行：

```bash
# 1. 全量编译（会编译所有模块，最终产出 yudao-server.jar）
mvn clean package -DskipTests

# 2. 或只编译 server 及其依赖
mvn clean package -pl yudao-server -am -DskipTests
```

打包完成后，可执行 jar 位于：

```
yudao-server/target/yudao-server.jar
```

---

## 三、配置文件位置与修改项

### 3.1 配置文件所在目录

```
yudao-server/src/main/resources/
├── application.yaml          # 主配置（一般不改）
└── application-local.yaml    # 当前激活的 profile（本地/部署都常用）
```

`application.yaml` 中有：

```yaml
spring:
  profiles:
    active: local   # 使用 application-local.yaml
```

部署时通常继续用 `local`，或新建 `application-prod.yaml` 并改为 `active: prod`。

### 3.2 必须修改的配置（application-local.yaml）

| 配置项 | 当前值 | 部署建议 | 行号约 |
|--------|--------|----------|--------|
| **MySQL 地址** | `127.0.0.1:3306` | 改为 `8.155.49.186:端口` 或 `localhost:映射端口` | 49、67 |
| **MySQL 用户名** | `root` | 按实际 | 57、69 |
| **MySQL 密码** | `1207` | 改为强密码 | 58、70 |
| **Redis 地址** | `127.0.0.1` | 改为 `8.155.49.186` 或 `localhost` | 84 |
| **Redis 端口** | `6379` | 若映射到其它端口则改 | 85 |
| **Redis 密码** | 无 | 生产建议开启并配置 | 86 |
| **服务端口** | `48080` | 可保持或按需改 | 2 |

### 3.3 修改示例（MySQL/Redis 在 8.155.49.186）

**场景 A：MySQL、Redis 都在该服务器，Docker 映射端口为 3307、6380**

```yaml
# 数据库
spring:
  datasource:
    dynamic:
      datasource:
        master:
          url: jdbc:mysql://8.155.49.186:3307/ruoyi-vue-pro?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&nullCatalogMeansCurrent=true&rewriteBatchedStatements=true
          username: root
          password: 你的密码
        slave:
          lazy: true
          url: jdbc:mysql://8.155.49.186:3307/ruoyi-vue-pro?...
          username: root
          password: 你的密码

  data:
    redis:
      host: 8.155.49.186
      port: 6380
      password: 你的Redis密码   # 若有
```

**场景 B：应用与 MySQL/Redis 在同一台机，用 localhost**

若 MySQL 容器映射 `3307:3306`，Redis 映射 `6380:6379`：

```yaml
url: jdbc:mysql://127.0.0.1:3307/ruoyi-vue-pro?...
redis:
  host: 127.0.0.1
  port: 6380
```

---

## 四、端口映射的影响（重要）

**结论：端口映射本身不影响应用，只要应用配置里的地址和端口正确即可。**

| 情况 | 说明 |
|------|------|
| MySQL 容器 `-p 3307:3306` | 宿主机 3307 → 容器内 3306，应用连 `host:3307` |
| Redis 容器 `-p 6380:6379` | 宿主机 6380 → 容器内 6379，应用连 `host:6380` |
| 宿主机 3306 已被占用 | 用 3307 等其它端口映射，应用配置用 3307 |

**原则：应用配置中的 `host:port` = 应用能访问到的 MySQL/Redis 地址和端口。**

- 应用在宿主机运行：用 `127.0.0.1:映射端口` 或 `8.155.49.186:映射端口`
- 应用在 Docker 中、MySQL/Redis 在宿主机：用 `host.docker.internal:映射端口`（部分环境）或宿主机 IP

---

## 五、部署方式

### 方式一：直接运行 jar（适合先快速验证）

```bash
# 1. 打包
mvn clean package -pl yudao-server -am -DskipTests

# 2. 上传 yudao-server/target/yudao-server.jar 到服务器

# 3. 运行（确保 MySQL、Redis 已启动且配置正确）
java -jar yudao-server.jar --spring.profiles.active=local

# 或指定 JVM 参数
java -Xms512m -Xmx512m -jar yudao-server.jar --spring.profiles.active=local
```

### 方式二：Docker 运行后端（推荐）

> 项目 Dockerfile 使用 `eclipse-temurin:21-jre`，JDK 21 可运行 JDK 17 编译的 jar。若希望与项目一致，可将 Dockerfile 首行改为 `FROM eclipse-temurin:17-jre`。

```bash
# 1. 在项目根目录打包
mvn clean package -pl yudao-server -am -DskipTests

# 2. 构建镜像（在 yudao-server 目录下，或指定 context）
cd yudao-server
docker build -t yudao-server:latest .

# 3. 运行容器（通过环境变量覆盖数据库、Redis 地址）
docker run -d \
  --name yudao-server \
  -p 48080:48080 \
  -e SPRING_PROFILES_ACTIVE=local \
  -e ARGS="--spring.datasource.dynamic.datasource.master.url=jdbc:mysql://8.155.49.186:3307/ruoyi-vue-pro?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&nullCatalogMeansCurrent=true --spring.datasource.dynamic.datasource.master.username=root --spring.datasource.dynamic.datasource.master.password=你的密码 --spring.data.redis.host=8.155.49.186 --spring.data.redis.port=6380" \
  yudao-server:latest
```

### 方式三：使用项目自带的 docker-compose

项目中有 `script/docker/docker-compose.yml`，可一键启动 MySQL、Redis、后端、前端。

若 MySQL/Redis 端口与宿主机冲突，可修改 compose 中的端口映射，例如：

```yaml
mysql:
  ports:
    - "3307:3306"   # 宿主机 3307 映射到容器 3306
redis:
  ports:
    - "6380:6379"
```

然后通过环境变量把数据库、Redis 地址传给 server 容器（compose 中已支持）。

---

## 六、部署前检查清单

- [ ] 已执行建表 SQL：`ruoyi-vue-pro.sql`、`quartz.sql`、`bpm-tables.sql`、IM 三张表
- [ ] MySQL、Redis 已启动且应用能连通
- [ ] `application-local.yaml` 中数据库、Redis 地址、端口、密码正确
- [ ] 防火墙已放行 48080（后端）、8080（前端，若用 compose）
- [ ] 生产环境建议关闭 `yudao.captcha.enable` 或配置真实验证码；关闭 `yudao.security.mock-enable`

---

## 七、常见问题

| 问题 | 可能原因 | 处理 |
|------|----------|------|
| 启动报数据库连接失败 | 地址、端口、密码错误 | 核对 `application-local.yaml` 或 ARGS 环境变量 |
| 连接 Redis 超时 | Redis 未启动或端口/防火墙 | 检查 Redis 状态、端口、防火墙 |
| 端口被占用 | 48080 已被占用 | 修改 `server.port` 或容器映射 |
| Docker 内应用连不上 MySQL | 用了 127.0.0.1 | 改用宿主机 IP 或容器名（同一 compose 时） |

---

## 八、简要流程总结

1. **改配置**：编辑 `yudao-server/src/main/resources/application-local.yaml`，填写 MySQL、Redis 地址、端口、密码。
2. **打包**：`mvn clean package -pl yudao-server -am -DskipTests`。
3. **部署**：上传 `yudao-server/target/yudao-server.jar` 到服务器，或用 Docker 构建运行。
4. **验证**：访问 `http://8.155.49.186:48080`，能打开登录页即表示后端正常。
